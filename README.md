# 我的应用官网

这是一个使用 Next.js 构建的现代化应用官网，包含产品介绍、用户协议和隐私政策页面。

## 功能特色

- 🎨 现代化的响应式设计
- 📱 移动端友好的导航菜单
- 🌙 支持深色模式
- ⚡ 基于 Next.js 15 和 Tailwind CSS 4
- 🔧 TypeScript 支持
- 📄 完整的法律文档页面

## 页面结构

- **首页** (`/`) - 应用介绍和核心特色展示
- **产品介绍** (`/product`) - 详细的产品功能说明
- **用户协议** (`/terms`) - 完整的用户服务协议
- **隐私政策** (`/privacy`) - 数据收集和使用政策

## 开发环境设置

### 前置要求

- Node.js 18.0 或更高版本
- npm、yarn、pnpm 或 bun

### 安装依赖

```bash
npm install
# 或
yarn install
# 或
pnpm install
```

### 启动开发服务器

```bash
npm run dev
# 或
yarn dev
# 或
pnpm dev
```

打开 [http://localhost:3000](http://localhost:3000) 查看网站。

### 构建生产版本

```bash
npm run build
# 或
yarn build
# 或
pnpm build
```

### 启动生产服务器

```bash
npm run start
# 或
yarn start
# 或
pnpm start
```

## 部署选项

### 1. Vercel 部署（推荐）

这是最简单的部署方式：

1. 将代码推送到 GitHub 仓库
2. 在 [Vercel](https://vercel.com) 上导入项目
3. Vercel 会自动检测 Next.js 项目并进行部署

### 2. Netlify 部署

1. 将代码推送到 GitHub 仓库
2. 在 [Netlify](https://netlify.com) 上连接 GitHub 仓库
3. 设置构建命令：`npm run build`
4. 设置发布目录：`.next`

### 3. 自托管部署

```bash
# 构建项目
npm run build

# 启动生产服务器
npm run start
```

## 技术栈

- **框架**: Next.js 15 (App Router)
- **样式**: Tailwind CSS 4
- **语言**: TypeScript
- **字体**: Geist Sans & Geist Mono
- **构建工具**: Turbopack

## 项目结构

```
my-app-website/
├── src/
│   ├── app/
│   │   ├── layout.tsx          # 根布局
│   │   ├── page.tsx            # 首页
│   │   ├── product/
│   │   │   └── page.tsx        # 产品介绍页
│   │   ├── terms/
│   │   │   └── page.tsx        # 用户协议页
│   │   └── privacy/
│   │       └── page.tsx        # 隐私政策页
│   └── components/
│       └── Navigation.tsx      # 导航组件
├── public/                     # 静态资源
├── package.json
└── README.md
```

## 自定义配置

### 修改网站信息

在 `src/app/layout.tsx` 中修改网站标题和描述：

```typescript
export const metadata: Metadata = {
  title: "您的应用名称",
  description: "您的应用描述",
};
```

### 修改导航链接

在 `src/components/Navigation.tsx` 中修改导航菜单项。

### 修改样式主题

项目使用 Tailwind CSS，您可以在各个组件中修改样式类名来自定义外观。

## 浏览器支持

- Chrome (最新版本)
- Firefox (最新版本)
- Safari (最新版本)
- Edge (最新版本)

## 许可证

MIT License

## 联系方式

如有问题或建议，请联系：
- 邮箱: support@cloudmagicmirror.com
