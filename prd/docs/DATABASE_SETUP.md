# 云开发数据库集合创建指南

本文档提供在微信云开发控制台中创建所有数据库集合的详细步骤。

## 📋 创建步骤

### 1. 进入云开发控制台
1. 打开[微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 打开你的小程序项目
3. 点击顶部菜单「云开发」→「数据库」
4. 进入「数据库」页面

### 2. 创建集合
在数据库页面，点击「+」按钮创建新集合，按照以下顺序创建：

---

## 🗄️ 核心业务集合（5个）

### 集合 1: `users` - 用户表

**集合名称**: `users`

**字段说明**:
- `_id`: 主键（系统自动生成，无需手动创建）
- `_openid`: 微信 OPENID（系统字段，自动填充，无需手动创建）
- `nickname`: String - 昵称
- `avatar_url`: String - 头像 URL
- `phone`: String - 手机号（可选）
- `created_at`: Date - 创建时间
- `updated_at`: Date - 更新时间

**权限设置**:
- 所有用户可读
- 仅创建者可写

**索引建议**:
- `_openid`（系统自动创建）
- ⚠️ **不需要为 `nickname` 创建索引**：业务逻辑中所有查询都通过 `_openid` 进行，`nickname` 仅用于展示，不作为查询条件

---

### 集合 2: `wishes` - 愿望表

**集合名称**: `wishes`

**字段说明**:
- `_id`: 主键（系统自动生成）
- `_openid`: 用户 OPENID（系统字段，自动填充）
- `beneficiary_type`: String - 许愿人/受益人类型（self/family/child/couple/other）
- `beneficiary_desc`: String - 受益人具体说明
- `deity`: String - 对象（向谁许愿）
- `wish_text`: String - 愿望原文
- `time_range`: String - 时间范围
- `target_quantify`: String - 目标量化
- `way_boundary`: String - 方式边界
- `action_commitment`: String - 行动承诺
- `return_wish`: String - 还愿/回向
- `status`: Number - 状态（0:未成功 1:已成功）
- `created_at`: Date - 创建时间
- `updated_at`: Date - 更新时间

**权限设置**:
- 仅创建者可读写

**索引建议**:
- `_openid`（系统自动创建）
- `status`（手动创建：数据库 → 索引 → 新建索引）
- `created_at`（手动创建：数据库 → 索引 → 新建索引）

---

### 集合 3: `analyses` - 分析记录表

**集合名称**: `analyses`

**字段说明**:
- `_id`: 主键（系统自动生成）
- `_openid`: 用户 OPENID（系统字段，自动填充）
- `wish_id`: String - 关联愿望 ID（可选）
- `wish_text`: String - 愿望原文
- `deity`: String - 对象（可选）
- `analysis_result`: Object - 分析结果（JSON 对象）
  - `missing_elements`: Array - 缺失要素
  - `possible_reasons`: Array - 失败原因
  - `failure_case`: String - 失败案例
  - `correct_posture`: String - 正确姿势
- `full_result`: Object - 完整结果（解锁后，JSON 对象）
  - `optimized_text`: String - 优化后的许愿稿
  - `structured_suggestion`: Object - 结构化建议
  - `steps`: Array - 步骤
  - `warnings`: Array - 警告
- `unlocked`: Boolean - 是否已解锁
- `unlock_token`: String - 解锁 token
- `unlock_token_expires_at`: Date - token 过期时间
- `unlock_token_used`: Boolean - token 是否已使用
- `created_at`: Date - 创建时间

**权限设置**:
- 仅创建者可读写

**索引建议**:
- `_openid`（系统自动创建）
- `wish_id`（手动创建）
- `unlock_token`（手动创建）
- `created_at`（手动创建）

---

### 集合 4: `orders` - 订单表

**集合名称**: `orders`

**字段说明**:
- `_id`: 主键（系统自动生成）
- `_openid`: 用户 OPENID（系统字段，自动填充）
- `wish_id`: String - 关联愿望 ID
- `amount`: Number - 金额（分，整数）
- `status`: Number - 状态（0:待支付 1:已支付 2:已完成 3:已退款）
- `payment_id`: String - 微信支付订单号
- `out_trade_no`: String - 商户订单号（唯一）
- `transaction_id`: String - 微信支付交易号
- `callback_received`: Boolean - 是否已收到回调
- `created_at`: Date - 创建时间
- `updated_at`: Date - 更新时间

**权限设置**:
- 仅创建者可读写

**索引建议**:
- `_openid`（系统自动创建）
- `status`（手动创建）
- `out_trade_no`（手动创建，唯一索引）
- `created_at`（手动创建）

---

### 集合 5: `unlock_logs` - 解锁日志表

**集合名称**: `unlock_logs`

**字段说明**:
- `_id`: 主键（系统自动生成）
- `_openid`: 用户 OPENID（系统字段，自动填充）
- `analysis_id`: String - 分析记录 ID
- `unlock_type`: String - 解锁类型（ad/share，可选）
- `device_fingerprint`: String - 设备指纹（可选）
- `created_at`: Date - 创建时间

**权限设置**:
- 仅创建者可读写

**索引建议**:
- `_openid`（系统自动创建）
- `analysis_id`（手动创建）
- `created_at`（手动创建）

---

## 🗄️ 扩展功能集合（3个）

### 集合 6: `wish_profiles` - 许愿人/受益人和对象信息表

**集合名称**: `wish_profiles`

**字段说明**:
- `_id`: 主键（系统自动生成）
- `_openid`: 用户 OPENID（系统字段，自动填充）
- `beneficiary_type`: String - 许愿人/受益人类型
- `beneficiary_desc`: String - 受益人具体说明
- `deity`: String - 对象（向谁许愿）
- `created_at`: Date - 创建时间
- `updated_at`: Date - 更新时间

**权限设置**:
- 仅创建者可读写

**索引建议**:
- `_openid`（系统自动创建）
- `updated_at`（手动创建）

---

### 集合 7: `persons` - 人员信息表

**集合名称**: `persons`

**字段说明**:
- `_id`: 主键（系统自动生成）
- `_openid`: 用户 OPENID（系统字段，自动填充）
- `name`: String - 姓名
- `category`: String - 分类（self/family/child/couple/other 或自定义）
- `id_card`: String - 身份证号（可选）
- `phone`: String - 手机号（可选）
- `created_at`: Date - 创建时间
- `updated_at`: Date - 更新时间

**权限设置**:
- 仅创建者可读写

**索引建议**:
- `_openid`（系统自动创建）
- `category`（手动创建）
- `updated_at`（手动创建）

---

### 集合 8: `person_categories` - 人员分类表

**集合名称**: `person_categories`

**字段说明**:
- `_id`: 主键（系统自动生成）
- `_openid`: 用户 OPENID（系统字段，自动填充）
- `value`: String - 分类值（唯一标识）
- `label`: String - 分类名称（显示）
- `icon`: String - 图标（可选）
- `is_default`: Boolean - 是否为默认分类
- `created_at`: Date - 创建时间
- `updated_at`: Date - 更新时间

**权限设置**:
- 仅创建者可读写

**索引建议**:
- `_openid`（系统自动创建）
- `value`（手动创建）
- `created_at`（手动创建）

---

## 📝 创建索引步骤

1. 进入云开发控制台 → 数据库
2. 选择对应的集合
3. 点击「索引」标签页
4. 点击「新建索引」
5. 选择要创建索引的字段
6. 设置索引类型（普通索引/唯一索引）
7. 点击「创建」

### 推荐创建的索引

#### `wishes` 集合
- 索引名称: `idx_status`
  - 字段: `status`（升序）
- 索引名称: `idx_created_at`
  - 字段: `created_at`（降序）

#### `analyses` 集合
- 索引名称: `idx_wish_id`
  - 字段: `wish_id`（升序）
- 索引名称: `idx_unlock_token`
  - 字段: `unlock_token`（升序）
- 索引名称: `idx_created_at`
  - 字段: `created_at`（降序）

#### `orders` 集合
- 索引名称: `idx_status`
  - 字段: `status`（升序）
- 索引名称: `idx_out_trade_no`（唯一索引）
  - 字段: `out_trade_no`（升序）
- 索引名称: `idx_created_at`
  - 字段: `created_at`（降序）

#### `unlock_logs` 集合
- 索引名称: `idx_analysis_id`
  - 字段: `analysis_id`（升序）
- 索引名称: `idx_created_at`
  - 字段: `created_at`（降序）

#### `wish_profiles` 集合
- 索引名称: `idx_updated_at`
  - 字段: `updated_at`（降序）

#### `persons` 集合
- 索引名称: `idx_category`
  - 字段: `category`（升序）
- 索引名称: `idx_updated_at`
  - 字段: `updated_at`（降序）

#### `person_categories` 集合
- 索引名称: `idx_value`
  - 字段: `value`（升序）
- 索引名称: `idx_created_at`
  - 字段: `created_at`（升序）

---

## ✅ 验证清单

创建完成后，请验证以下内容：

- [ ] 已创建 8 个集合（users、wishes、analyses、orders、unlock_logs、wish_profiles、persons、person_categories）
- [ ] 所有集合的权限设置为「仅创建者可读写」
- [ ] 已创建必要的索引（参考上方索引建议）
- [ ] 测试插入一条数据，确认字段类型正确

---

## 🔍 测试数据示例

### 测试 `users` 集合
```json
{
  "nickname": "测试用户",
  "avatar_url": "https://example.com/avatar.jpg",
  "phone": "13800138000",
  "created_at": "2025-01-01T00:00:00.000Z",
  "updated_at": "2025-01-01T00:00:00.000Z"
}
```

### 测试 `wishes` 集合
```json
{
  "beneficiary_type": "self",
  "deity": "观音菩萨",
  "wish_text": "希望找到好工作",
  "status": 0,
  "created_at": "2025-01-01T00:00:00.000Z",
  "updated_at": "2025-01-01T00:00:00.000Z"
}
```

---

## 📚 注意事项

1. **系统字段**: `_id` 和 `_openid` 是系统自动生成的字段，无需手动创建
2. **字段类型**: 云数据库会自动识别字段类型，首次插入数据时会自动创建字段
3. **权限设置**: 建议所有集合都设置为「仅创建者可读写」，确保数据安全
4. **索引优化**: 索引可以提高查询性能，但也会占用存储空间，建议按需创建
5. **数据备份**: 定期备份重要数据，避免数据丢失

---

## 🆘 常见问题

### Q: 如何修改集合权限？
A: 在集合列表中，点击集合名称进入详情页，点击「权限设置」进行修改。

### Q: 如何删除集合？
A: 在集合列表中，点击集合名称进入详情页，点击「删除集合」按钮（注意：删除后数据无法恢复）。

### Q: 索引创建失败怎么办？
A: 检查字段名称是否正确，确保字段已存在数据，或先插入一条测试数据再创建索引。

### Q: 如何查看集合中的数据？
A: 在集合列表中，点击集合名称进入详情页，点击「数据」标签页即可查看。

---

## 📞 需要帮助？

如果遇到问题，请参考：
- [微信云开发文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)
- [云数据库文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/database.html)

---

**创建日期**: 2025-01-XX  
**最后更新**: 2025-01-XX
